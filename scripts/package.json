{
  "name": "flashblocks-transaction-sender",
  "version": "1.0.0",
  "description": "Send transactions on Base Sepolia with Flashblocks pre-confirmations",
  "type": "module",
  "main": "send-transaction.ts",
  "scripts": {
    "send": "bun run send-transaction.ts",
    "build": "tsc send-transaction.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}

